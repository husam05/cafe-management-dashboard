const fs = require('fs'); const path = require('path'); const dataPath = path.join(process.cwd(), '../cafe_management.json'); console.log('Reading from:', dataPath); try { const content = fs.readFileSync(dataPath, 'utf8'); const json = JSON.parse(content); const categories = json.find(t => t.name === 'Categories'); console.log('Categories found:', categories ? categories.data.length : 0); if(categories) console.log('First Category:', categories.data[0]); } catch (e) { console.error('Error:', e.message); }
